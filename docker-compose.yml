services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: old_school_bird
      POSTGRES_USER: bird
      POSTGRES_PASSWORD: bird
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
